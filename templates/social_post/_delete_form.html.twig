<form method="post" action="{{ path('app_social_post_delete', {'id': social_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ social_post.id) }}">
    <button class="btn">Delete</button>
</form>
